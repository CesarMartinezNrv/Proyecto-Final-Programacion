# Contribuciones

## Cesar Martinez

Responsable del modulo de estructura del sistema:

- Diseno e implementacion de `Persona` (clase base abstracta), `Cliente` y `Tecnico` (herencia y polimorfismo).
- Diseno e implementacion de `Dispositivo` e `HistorialServicio`, incluyendo `operator==` y `operator<<`.
- Diseno e implementacion de `GestorClientes`, `GestorTecnicos` y `GestorDispositivos` con arreglos dinamicos (`new`/`delete[]`, redimensionamiento, `nullptr`).
- Persistencia en archivos `.txt` (guardado y carga) para las 8 clases.
- Menu principal (`main.cpp`) que integra los gestores.


